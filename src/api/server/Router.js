/**
 * Router class
 * Used to set endpoints and respective controllers
 */
export class Router {
  /**
   * apply method
   * Used to apply routes
   * @param {Request} req
   * @param {Response} res
   */
  apply(req, res) {

  }
}
